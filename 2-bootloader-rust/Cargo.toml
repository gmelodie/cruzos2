[package]
name = "bootloader"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]  # produce .o for linking manually
path = "src/lib.rs"

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"

